# app/models/registry
# Serialized ML model artifacts (joblib / pickle) are stored here.
# This directory is intentionally kept out of version control for large binaries
# — add *.pkl to .gitignore and distribute via a model registry or DVC.
